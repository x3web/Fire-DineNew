# Complete correction report

This package implements the 23 August release-gate corrections while preserving the Product Guide catalogue: exactly three active roots, protected approved children, product assignments, approved names/content/assets, exact confirmed prices and pending unknown prices.

Implemented corrections include: the new checksummed `20260823_fire_dine_release_gate_fixes.sql` migration and mirrored clean-install changes; corrected installation, curing, operation and maintenance guidance; optional tax configuration disabled by default; complete persisted quote/customer/configuration/pending-price/status data; quote list/detail/history and detailed customer/business emails; persisted contact enquiries with protected list/detail/status administration and failure-safe email logging; exact Product 98 Rib Rack relation; archived Products 83 and 106; out-of-stock Product 104; disabled broken sunset gallery record because its exact asset was not supplied; restored exact supplied Bebas Neue and referenced website assets; quantity-capped basket merging with recalculated totals; protected category identity/parent/status and accurate old/new path redirects; public 404 handling for invalid category pairs; explicit public API field whitelisting with recursive sanitization; `.env.test`-only test configuration and destructive database-name safeguards; and updated automated regression coverage.

The application contains product administration and existing media synchronization controls used by product editing. No unsupported claim of a separate full media-library CRUD system is made.

Locally available source and packaging checks are recorded in `AUTOMATED-TEST-RESULTS.md`. Live-server checks are listed separately in `PENDING-LIVE-SERVER-VERIFICATION.md`; their pending status does not indicate omitted application code.

The final release-gate follow-up also corrects the catalogue PHPUnit column/slug assertions; adds the internal-only absolute admin quote URL and verifies it is absent from the customer email; covers tax-disabled and configurable-tax calculations; renders and tests quote notes/options; completes enquiry application, measurement, preferred-contact and notification-log details with escaped output; tests enquiry status changes; makes browser-admin credentials mandatory with isolated administrator setup/cleanup; converts `/api/brochure.php` into a consistent 301 redirect in PHP, Apache and Nginx; expands the browser crawl across all public routes and local asset classes; hides interactive administrator passwords; and prevents caching of every admin HTML/JSON response.
